# Boosted Hybrid Notebook 可落地改造清单

来源：`store-sales-forecasting-with-boosted-hybrid-model.ipynb`

## 1. 这份 notebook 的核心思路
- 先用线性模型拟合趋势和周期
- 再用树模型拟合残差
- 对某些特殊业务现象做单独规则处理
- 按天滚动预测，而不是一次性全量预测

## 2. 可以直接借鉴的部分

### 2.1 Zero forecasting
- 找出历史上始终不卖的 `store_nbr + family`
- 对这类组合，在测试集里优先直接预测 0
- 对于测试期突然开始促销的组合，单独排除，避免误判

### 2.2 油价处理
- `oil.csv` 先做日期补齐
- 对缺失油价做线性插值
- 再派生：
  - `oil_diff_1`
  - `oil_pct_change_1`
  - `expensive_oil`

### 2.3 发薪日特征
- 显式加入：
  - 是否 15 日
  - 是否月末
  - 距离下一个发薪日还有几天
  - 距离上一个发薪日过去几天
- 这是这个比赛里非常常见且稳定的业务特征

### 2.4 地震事件特征
- 2016-04-16 前后加入特殊事件标签
- 可做：
  - 事件日标记
  - 事件后若干天衰减标记
  - 特定商品组增强标签

### 2.5 更严格的时间切分
- 不随机切分
- 保持按时间往后切
- 尽量让验证期更像真正测试期

## 3. 值得考虑，但不要急着全抄

### 3.1 线性模型 + 残差树模型
- 第一步用线性模型拟合趋势 / 周期
- 第二步用 XGBoost 或 LightGBM 拟合残差
- 适合作为“创新点”或第二阶段提分方案

### 3.2 逐日递推预测
- 测试集 15 天不要一次性全扔给模型
- 可以按天预测，预测结果回填到下一天的 lag 特征里
- 这个方法更贴近真实时序推断

### 3.3 季节项的显式建模
- 可尝试 Fourier 项或 DeterministicProcess
- 重点用于周周期、月周期、年周期
- 对线性部分更友好

## 4. 暂时不建议照搬的部分
- 直接复刻 notebook 的全部参数
- 直接照搬它的训练时间窗口
- 直接沿用它的 validation 结论
- 直接把 notebook 的特征全塞进当前项目

原因：
- 这类 notebook 往往强依赖它自己的数据切法
- 我们当前版本已经有一套可运行 baseline，应该增量改造
- 太多规则会让项目变难解释，也更难排查问题

## 5. 跟我们当前项目的接入顺序

### 第一优先级
1. `zero forecasting`
2. `oil interpolation`
3. `payday` 特征
4. `earthquake` 特征

### 第二优先级
5. 更严格的时间验证
6. 逐日递推预测
7. 线性趋势 + 树残差混合模型

### 第三优先级
8. Fourier / DeterministicProcess
9. 更复杂的融合和 stacking

## 6. 对当前项目的实际建议
- 先保留现有 LightGBM baseline
- 再加 `zero forecasting`
- 再补 `payday` 和 `earthquake`
- 如果时间够，再试 hybrid model

## 7. 一句话总结
这份 notebook 最有价值的不是“模型名字”，而是它把几个业务上很强的点单独拎出来处理了。  
我们下一步最适合做的是：**先吸收业务规则，再考虑混合模型。**
